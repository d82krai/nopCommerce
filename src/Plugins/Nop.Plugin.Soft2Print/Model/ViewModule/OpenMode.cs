namespace Nop.Plugin.Soft2Print.Model.ViewModule
{
    public enum OpenMode
    {
        /// <summary>
        /// Use this to go from a page that opens the iframe an zooms in so that it runs full screen
        /// </summary>
        EaseInFullScreen = 11,
        /// <summary>
        /// Normal full screen
        /// </summary>
        FullScreen = 21

    }
}
